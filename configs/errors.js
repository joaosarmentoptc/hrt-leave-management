const USER_NOT_AUTHENTICATED = "User is not authenticated";
const LEAVE_ALREADY_BOOKED = "There is a leave already scheduled for that date";
const END_DATE_AFTER_START_DATE = "end date cannot be before the start date";
const CANNOT_APPROVE_OR_REJECT_LEAVE_REQUEST =
  "You cannot approve or reject this leave request";

module.exports = {
  USER_NOT_AUTHENTICATED,
  LEAVE_ALREADY_BOOKED,
  END_DATE_AFTER_START_DATE,
  CANNOT_APPROVE_OR_REJECT_LEAVE_REQUEST,
};
